#include <iostream>
#include <vector>

int main(){
  int n, l;
  std::cin >> n >> l;
  std::vector<int> a(n);
  for(int i = 0; i < n; ++i){
    std::cin >> a[i];
  }
  double vmin = std::max(a[i])
  return 0;
}
