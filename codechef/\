#include <stdio.h>

int main(void){
	int t;
	scanf("%d", &t);
	while(t--){
		int u, v, a, s;
		scanf("%d %d %d %d", &u, &v, &a, &s);
		long long int excessSpeed = u*u - 2*a*s - v*v;
		if(excessSpeed > 0)
			printf("No\n");
		else
			printf("Yes\n");
	}
	return 0;
}
