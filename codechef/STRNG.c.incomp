#include <stdio.h>
#include <stdlib.h>

int array_gcd(const int *arr, const int n){
    int gcd = 0;
    return gcd;
}

int main(void){
    int t;
    scanf("%d", &t);
    while(t--){
        int n;
        scanf("%d", &n);
        int *arr = (int *)malloc(sizeof(int) * n);
        for(int i = 0; i < n; ++i)
            scanf("%d", &arr[i]);
        const int gcd = array_gcd(arr, n);
        if(gcd == 1)
            printf("")
        free(arr);
    }
    return 0;
}
